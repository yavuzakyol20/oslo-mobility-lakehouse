apiVersion: v1
kind: Service

metadata:
  name: mobility-dashboard-service
  namespace: oslo-mobility

spec:
  selector:
    app: mobility-dashboard

  ports:
    - port: 80
      targetPort: 80

  type: NodePort
