apiVersion: apps/v1
kind: Deployment

metadata:
  name: mobility-dashboard-deployment
  namespace: oslo-mobility

spec:
  replicas: 1

  selector:
    matchLabels:
      app: mobility-dashboard

  template:
    metadata:
      labels:
        app: mobility-dashboard

    spec:
      containers:
        - name: dashboard
          image: oslo-mobility-dashboard:latest
          imagePullPolicy: Never

          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"

          envFrom:
            - configMapRef:
                name: dashboard-config
          env:
            - name: STORAGE_ACCOUNT
              valueFrom:
                secretKeyRef:
                  name: dashboard-secret
                  key: STORAGE_ACCOUNT

          ports:
            - containerPort: 8501

          livenessProbe:
            httpGet:
              path: /
              port: 8501
            initialDelaySeconds: 30
            periodSeconds: 15
            timeoutSeconds: 5
            failureThreshold: 3

          readinessProbe:
            httpGet:
              path: /
              port: 8501
            initialDelaySeconds: 15
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
