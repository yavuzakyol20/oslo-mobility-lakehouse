apiVersion: apps/v1
kind: Deployment

metadata:
  name: mobility-dashboard
  namespace: default

spec:
  replicas: 1

  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1

  selector:
    matchLabels:
      app: mobility-dashboard

  template:
    metadata:
      labels:
        app: mobility-dashboard

    spec:
      containers:
        - name: dashboard
          image: oslo-mobility-dashboard:latest
          imagePullPolicy: Never

          ports:
            - containerPort: 8501

          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"

          envFrom:
            - configMapRef:
                name: mobility-dashboard-config

          env:
            - name: STORAGE_ACCOUNT
              valueFrom:
                secretKeyRef:
                  name: dashboard-secret
                  key: STORAGE_ACCOUNT

          readinessProbe:
            httpGet:
              path: /
              port: 8501
            initialDelaySeconds: 15
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3

          livenessProbe:
            httpGet:
              path: /
              port: 8501
            initialDelaySeconds: 30
            periodSeconds: 15
            timeoutSeconds: 5
            failureThreshold: 3
